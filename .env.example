# Weather Dashboard Environment Variables
# Currently no environment variables are needed as we use the free Open-Meteo API
# This file is included for future use if API keys or other config is needed

# Example for future use:
# WEATHER_API_KEY=your_api_key_here
# NEXT_PUBLIC_APP_URL=http://localhost:3000